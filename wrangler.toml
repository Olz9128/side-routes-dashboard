# Cloudflare Pages configuration.
# pages_build_output_dir is what tells Cloudflare to deploy as Pages
# (with auto-detected functions/) rather than as a single Worker.

name = "side-routes-dashboard"
compatibility_date = "2026-01-01"
pages_build_output_dir = "public"
